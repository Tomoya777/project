# C6 M2 顧客情報登録部 熊谷
# C2 user_searchから来たユーザ名が使用されているか確認する

import user_search  # c2m2
from pathlib import Path
import os

# Build paths inside the project like this: BASE_DIR / 'subdir'.
# ベースディレクトリからのパスを通す
BASE_DIR = Path(__file__).resolve().parent.parent


def userdata_register(user_id, user_mail, passw_temp):

    pass
